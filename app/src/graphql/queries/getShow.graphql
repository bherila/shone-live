query GetShow($ID: String!) {
  show(showId: $ID) {
    id
    title
    image_url
    start_date
    end_date
    chatMessages {
      id
      timestamp
      message
      alias
      __typename
    }
  }
}
