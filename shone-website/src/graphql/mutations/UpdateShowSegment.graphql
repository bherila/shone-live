mutation UpdateShowSegment(
  $id: String!
  $title: String!
  $productsIds: [String!]!
) {
  updateShowSegment(
    data: {
      id: $id
      title: $title
      productsIds: $productsIds
    }
  ) {
    id
  }
}
